/**
 * @fileoverview Editor variant tokens shared by slash commands and toolbar presets.
 *
 * @module src/lib/nexusEditor/editorTypes
 */

/** Toolbar density presets for {@link NexusRichTextEditor}. */
export type NexusEditorVariant = "minimal" | "default" | "full";
